name: test build

on:
  pull_request:
    paths:
      - Dockerfile
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v3
    - name: test build
      uses: docker/build-push-action@v4
      with:
        push: false
