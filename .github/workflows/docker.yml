name: Docker push release

on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
    - name: Build with latest tag
      run: docker build . --file Dockerfile --tag tuetenk0pp/sharelatex-full:latest
    - name: push latest
      run: docker push tuetenk0pp/sharelatex-full:latest
    - name: Build with version tag
      run: docker build . --file Dockerfile --tag tuetenk0pp/sharelatex-full:$GITHUB_REF
    - name: push version
      run: docker push tuetenk0pp/sharelatex-full:latest
